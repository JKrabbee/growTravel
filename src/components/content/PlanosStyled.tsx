import { styled } from 'styled-components';

const PlanosStyled = styled.div`
  width: 100%;

  @media (max-width: 1024px) {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
`;

export default PlanosStyled;
